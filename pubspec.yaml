name: mysql
dependencies:
  build_native: ^0.0.9
  #build_native:
  #  path: ../build_native/build_native
  pool: ^1.0.0
  tuple: ^1.0.0
dev_dependencies:
  build_runner:
    git:
      url: https://github.com/thosakwe/build.git
      path: build_runner
  test: ^0.12.0